<Window x:Class="SeaNoteWPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" 
        WindowState="Maximized" WindowStyle="None" ResizeMode="NoResize"
        Background="{StaticResource BgColor}" AllowsTransparency="True"
        Loaded="Window_Loaded" MouseDown="Window_MouseDown">

    <Grid>
        <Ellipse Width="1000" Height="1000" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="-300,-300,0,0" Opacity="0.3" IsHitTestVisible="False">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#64FFDA" Offset="0"/>
                    <GradientStop Color="Transparent" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
            <Ellipse.Effect>
                <BlurEffect Radius="150"/>
            </Ellipse.Effect>
        </Ellipse>

        <Ellipse Width="800" Height="800" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-200,0,0,-200" Opacity="0.2" IsHitTestVisible="False">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#F59E0B" Offset="0"/>
                    <GradientStop Color="Transparent" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
            <Ellipse.Effect>
                <BlurEffect Radius="150"/>
            </Ellipse.Effect>
        </Ellipse>

        <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,20,30,0" 
                Click="BtnClose_Click" Background="Transparent" Foreground="White" 
                FontSize="24" BorderThickness="0" Cursor="Hand"/>

        <Border Width="400" Height="520" Background="#CC1C2541" CornerRadius="20" Padding="40" 
                VerticalAlignment="Center" HorizontalAlignment="Center" 
                BorderBrush="#33FFFFFF" BorderThickness="1">

            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="40" ShadowDepth="10" Opacity="0.5"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="SeaNote" Style="{StaticResource HeaderTitle}" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="Ship Management System" Foreground="#8D99AE" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,40" FontFamily="{StaticResource Raleway}"/>

                <TextBlock Text="Username" Style="{StaticResource GlassLabel}"/>
                <TextBox x:Name="tbUsername" Style="{StaticResource InputStyle}" Height="45" Margin="0,0,0,15"/>

                <TextBlock Text="Password" Style="{StaticResource GlassLabel}"/>
                <PasswordBox x:Name="tbPassword" Height="45" Margin="0,0,0,30" Background="#25304C" Foreground="White" BorderThickness="0" Padding="10,12" FontSize="14">
                    <PasswordBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </PasswordBox.Resources>
                </PasswordBox>

                <Button Content="LOGIN" Click="BtnLogin_Click" Width="Auto" Height="50" 
                        Background="{StaticResource BtnAddDefault}" Foreground="#0B132B" FontWeight="Bold" FontSize="18" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="25"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,0">
                    <TextBlock Text="No account? " Foreground="#8D99AE"/>
                    <TextBlock Text="Sign Up" Foreground="#F59E0B" FontWeight="Bold" Cursor="Hand" MouseLeftButtonDown="LnkSignUp_Click"/>
                    <TextBlock Text="  |  " Foreground="#8D99AE"/>
                    <TextBlock Text="Forgot Password?" Foreground="#F59E0B" FontWeight="Bold" Cursor="Hand" MouseLeftButtonDown="LnkForgot_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
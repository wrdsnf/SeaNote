<Page x:Class="SeaNoteWPF.Pages.OwnerMaintenancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="OwnerMaintenancePage">

    <Grid Margin="30,20,30,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Maintenance Log" Style="{StaticResource HeaderTitle}"/>
            <TextBlock Text="Laporan kondisi dan perbaikan aset" Foreground="#8D99AE" FontSize="14" Margin="2,5,0,0" FontFamily="{StaticResource Raleway}"/>
        </StackPanel>
        <Border Grid.Row="1" Background="#331C2541" CornerRadius="20" Padding="20" BorderBrush="#33FFFFFF" BorderThickness="1">
            <DataGrid x:Name="dgMaint" Style="{StaticResource GlassDataGrid}" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ship Name" Binding="{Binding ShipName}" Width="150"/>
                    <DataGridTextColumn Header="Issue" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Report Date" Binding="{Binding Date}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>